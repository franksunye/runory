# Runory Cloud 环境配置模板
# 复制为 .env.local 后填入真实值；.env.local 不会被提交（见 .gitignore）
#
# 架构共识：同一份代码同时支持本地 SQLite 与云端 Turso，零代码切换。
#   - 本地开发：仅需 LIBSQL_URL=file:... （或完全不配，默认使用本地文件）
#   - 云端生产：LIBSQL_URL=libsql://... + LIBSQL_AUTH_TOKEN=...
# 参考实现：sibling project fs-aol（apps/console/lib/db.ts 同款 Proxy 懒加载模式）

# ===== 数据库（@libsql/client，本地 SQLite / 云端 Turso 二选一）=====

# ── 模式 A：本地 SQLite（默认，零配置即可开发）──
# 不设任何 LIBSQL_* 变量时，默认使用 file:${cwd}/data/runory.db
# 如需指定路径，取消下一行注释：
# LIBSQL_URL=file:./data/runory.db

# ── 模式 B：云端 Turso（生产 / 共享数据库）──
# Turso 库与其它项目共享，Runory 平台表使用 runory_ 前缀隔离。
# 获取凭据：bash scripts/turso_setup.sh（从本地 sqlite 文件建库）
# LIBSQL_URL=libsql://your-runory-db.turso.io
# LIBSQL_AUTH_TOKEN=your_turso_auth_token

# ===== 表前缀（与共享 Turso 库的其它项目隔离）=====

# 平台表前缀（workspaces / object_definitions / ... 等 11 张元数据表）
# 默认 runory_，与 contracts/tables.json 中的 defaultPrefix 一致
# RUNORY_TABLE_PREFIX=runory_

# 业务表前缀（模块迁移创建的 customer / contact 等业务表）
# 默认空（无前缀）；如需进一步隔离可设为 runory_biz_ 等
# RUNORY_BUSINESS_TABLE_PREFIX=

# ===== MCP Server =====

# Runory Cloud API 基址（MCP stdio server 通过 HTTP fetch 调用 API）
# 本地开发默认 http://localhost:3000
# RUNORY_API_BASE=http://localhost:3000
