# v0.6 Command Architecture Stabilization TODO

| Metadata | Value |
| --- | --- |
| Status | `historical` |
| Topic | `architecture` |
| Applies to | `v0.6.0` |
| Owner | Product / Engineering |
| Last reviewed | 2026-07-20 |
| Supersedes | — |
| Superseded by | — |

Related documents:

- [v0.6 Foundation Architecture Stabilization Plan](./v0.6-foundation-architecture-stabilization-plan.md)
- [Contract-Driven Command Architecture](./contract-driven-command-architecture.md)
- [Product Roadmap](../product/product-roadmap.md)
- [v0.6 Deferred Work Handoff](./v0.6-deferred-work-handoff.md)
- [v0.5 Commercial FSM Technical Specification](../product/v0.5-commercial-fsm-technical-spec.md)

## 1. Purpose

The v0.5 Command migration corrected a real consistency problem: governed
business changes previously depended on feature code remembering every related
Assignment, Schedule, Workflow, Form, event, and audit update.

The architectural direction is accepted. This document records the remaining
implementation debt so the team can absorb the current model before changing
the kernel again. It is a v0.6 stabilization backlog, not authorization for an
immediate rewrite.

## 2. Frozen v0.5 Baseline

The following baseline is considered working and must remain compatible:

```text
37 executeCommand entry points
26 business Module-owned Command Contracts
11 Platform Service-owned Command Contracts (Workflow 6 + Forms 5)
workspace-scoped, versioned Contract snapshots
atomic semantic capability providers
idempotency, optimistic concurrency, audit and Domain Events
663 Platform Core regression tests passing at migration completion
```

Until v0.6 stabilization begins, changes to the Command kernel are limited to:

- security or tenant-isolation defects;
- proven data corruption or incomplete transaction defects;
- production-blocking compatibility or migration defects;
- the minimum change required to add a necessary v0.5 business Command.

Code organization, naming, generalization, and speculative extensibility alone
are not reasons to reopen the kernel during the digestion period.

## 3. Decisions That Must Be Preserved

These are platform invariants, not TODO items:

1. Named Commands remain the only entry point for governed business change.
2. UI, API, mobile, Workflow, Automation, MCP, Skills, and agents call the same
   Command rather than coordinating generic updates.
3. Business Modules and Platform Services are distinct Contract sources.
4. A Workspace resolves the exact Contract snapshot provisioned for it.
5. Cross-capability authoritative writes use declared consistency and a
   compatible Provider.
6. Workflow and Automation orchestrate Commands; they do not replace Command
   invariants or transactions.
7. Projections such as My Work, Planning, and Activity remain read models, not
   competing owners of lifecycle state.

## 4. Why Stabilization Is Still Required

The model is sound, but the first migration concentrated too many
responsibilities in a small number of implementation files. In addition, some
Contract declarations are validated structurally but are not yet fully
executable at Runtime.

The Command workstream objective within v0.6.0 is therefore:

> Preserve the semantics and compatibility of v0.5 while reducing accidental
> implementation complexity and closing the remaining enforcement gaps.

This is a refactor of boundaries and guarantees, not a redesign of the business
model.

## 5. v0.6 TODO Backlog

### A. Inventory and safety net — do first

- [x] Generate the current numerical baseline at stabilization start; do not
      treat the migration-completion counts above as the current repository
      inventory.
- [x] Generate a repository check proving every `executeCommand` key maps to
      exactly one provisionable Contract.
- [x] Detect duplicate Command ownership across Module and Platform Service
      manifests.
- [x] Detect declared Commands without a callable implementation.
- [x] Produce a generated inventory of Command, source, aggregate, transition,
      permission, effects, emitted events, and Provider version.
- [x] Add a frozen v0.5 Contract source/version fixture and verify existing
      Workspace snapshots through the diagnostics inventory.
- [x] Extend compatibility fixtures through Module and Platform Service upgrade
      paths.
- [x] Expose Workspace Contract source/version and resolved Provider versions
      through a read-only diagnostics inventory.
- [x] Persist the resolved Contract source/version and Provider versions on each
      Command execution record.

Exit gate: the current 37-Command baseline is machine-auditable before files are
reorganized.

### B. Split implementation responsibilities — behavior-preserving

- [x] Split the current Command Contract implementation by responsibility:

```text
command-contracts/
  schema-and-types
  registry
  workspace-resolution
  validation
  runtime-plan
  diagnostics
  providers/
    assignment
    scheduling
    workflow
    forms
    quote
    fsm
```

- [x] Keep Provider implementations with their bounded context or a clearly
      named integration adapter.
- [x] Remove circular or implicit provider-registration dependencies.
- [x] Define a single public API for Contract loading, validation, snapshotting,
      resolution, and execution planning.
- [x] Keep this slice behavior-neutral; no lifecycle or permission changes.

Exit gate: the full regression suite and snapshot compatibility fixtures pass
without changing observable Command results.

### C. Close Runtime enforcement gaps

- [x] Validate the actual source state against `transition.from` before writes.
- [x] Validate the resulting state against `transition.to` after handler and
      Provider preparation.
- [x] Replace critical free-text postconditions with typed, executable
      assertions; retain prose only as documentation.
- [x] Enforce Contract permission and actor policy through one Runtime
      authorization boundary.
- [x] Define a consistent concurrency policy for Form Submission revision,
      draft promotion, return, and acceptance.
- [x] Verify Provider cardinality against prepared and affected records rather
      than only the presence of SQL statements.
- [x] Add negative tests proving Workflow, Automation, and generic CRUD cannot
      bypass governed transitions.

Exit gate: a valid Contract is not merely descriptive; Runtime can reject an
implementation that violates its transition, authorization, effect, event,
audit, or critical postcondition semantics.

### D. Productize provisioning and upgrades

- [x] Sync Platform Service snapshots only during workspace provisioning,
      Platform Service upgrade, or explicit repair—not every ordinary Module
      installation.
- [x] Add an idempotent backfill/repair operation with dry-run diagnostics.
- [x] Persist and expose workspace Contract inventory for support diagnostics
      and future administration surfaces.
- [x] ~~Add standalone upgrade impact analysis for changed Commands.~~ Deferred
      until the real Module upgrade mutation flow has a product consumer;
      retain snapshot replacement and ownership-conflict tests.
- [x] Complete workspace backfill, then remove the two legacy process-level
      static Contract bridges.
- [x] Define uninstall behavior when another installed source consumes a
      capability or Command owned by the source being removed.

Exit gate: provisioning, upgrade, repair, and uninstall behavior is explicit,
observable, and safe for existing Workspaces.

### E. Developer experience and governance

- [x] Publish a short “How to add a governed Command” guide.
- [x] ~~Provide public SDK template and generated Contract-test APIs.~~ Deferred
      until an external Module author needs them; retain the concise guide,
      CLI validation, Catalog validation, and real handler fixtures.
- [x] Add Catalog/CI gates for capability closure and breaking Contract changes.
- [x] Add architecture tests preventing cross-domain physical SQL from leaking
      into unrelated handlers.
- [x] Document when an effect belongs inside one aggregate boundary versus a
      semantic Provider.
- [x] Add concise operational errors that identify Command, workspace source,
      missing capability, and remediation.

Exit gate: a Module developer can add a correct Command without understanding
the entire kernel or copying an existing handler blindly.

### F. Agent-first Workspace customization — parallel design track

This track protects Runory's original customization goal. It runs alongside
Command stabilization but must not be absorbed into the Command kernel
refactor.

- [x] Define three explicit customization tiers:
      workspace metadata and presentation, configurable process composition,
      and governed extension development.
- [x] Keep ordinary field, view, form, relation, navigation, and small custom
      object changes in Workspace Metadata rather than official manifests.
- [x] Let configurable Workflow change steps, assignees, conditions, required
      forms, SLA, and notifications while invoking existing Commands at
      governed business boundaries.
- [x] Require an Extension Module, Command Contract, and compatible Provider
      only when customization changes a core invariant or cross-aggregate
      authoritative transaction.
- [x] Design Agent-facing customization operations for draft, validation,
      impact preview, publish, audit, rollback, and version history.
- [x] Ensure end users and Agents do not need to edit YAML manifests or know
      physical table names.
- [x] Define the safe default capabilities of a small custom object: fields,
      relations, CRUD, permissions, views, search, audit, forms, and Workflow
      association.
- [x] Evaluate a versioned Custom Object State Machine Runtime for user-defined
      lifecycles without introducing an unrestricted generic status update.
- [x] Add architecture tests proving ordinary Workspace extensions survive
      Module upgrades and cannot override protected official invariants.
- [x] Provide Agent-readable discovery APIs for available objects, fields,
      Commands, current allowed actions, effects, and customization limits.

Exit gate: a user or authorized Agent can implement common SME customization
without code or manifest editing, while changes to authoritative business
semantics still pass the governed extension boundary.

## 6. Explicit Non-Goals

The stabilization effort must not:

- replace Commands with Workflow or Automation;
- introduce distributed transactions for local atomic facts;
- turn every ordinary field update into a Command;
- create one generic status-transition Command;
- move physical SQL into manifests;
- redesign all v0.5 FSM, Quote, Workflow, or Form lifecycles;
- add abstraction solely to support hypothetical future Packs;
- require ordinary Workspace customization to publish a business Module;
- expose physical manifests or SQL as the primary Agent customization API;
- block v0.6 product work on non-critical naming or formatting cleanup.

## 7. Proposed Execution Order

```text
Slice 1  Inventory, generated coverage checks, compatibility fixtures
Slice 2  Behavior-preserving file and Provider boundary split
Slice 3  Runtime transition, authorization, and postcondition enforcement
Slice 4  Provisioning, upgrade, repair, uninstall, legacy bridge removal
Slice 5  Agent-first Workspace customization contract and management APIs
Slice 6  SDK guidance, Catalog gates, diagnostics, final architecture review
```

Each slice must be independently reviewable and keep the product runnable. Do
not combine this stabilization with a large FSM feature or UI redesign.

## 8. Start Criteria

Begin the Command workstream when:

- the team can explain Command, Contract, Runtime, Provider, snapshot, Workflow,
  and projection ownership in consistent terms;
- the generated inventory work and compatibility-fixture design are scoped;
- a named owner and time box are assigned.

The first v0.6.0 slice establishes the acceptance and snapshot fixtures that
were previously listed as prerequisites. Security, consistency, migration, and
production-reliability corrections may still start earlier when necessary.

## 9. Completion Criteria

Completed on 2026-07-20. This closes the v0.6 Command and Workspace
customization architecture stabilization workstream; it does not by itself
declare the full v0.6 product release complete.

The v0.6 stabilization is complete when:

- all governed Commands have exactly one source and one Runtime plan;
- critical transition and postcondition semantics are executable;
- permissions are enforced consistently at the Runtime boundary;
- Provider selection and versions are diagnosable historically;
- the legacy static bridge is removed;
- workspace provisioning and upgrades do not perform surprising global repair;
- core files have clear bounded responsibilities;
- existing v0.5 journeys remain compatible and pass without data repair;
- the architecture can be explained to a new developer using one concise
  lifecycle example;
- common field, form, Workflow, and small custom-object changes remain
  available through versioned Workspace customization APIs without editing
  official manifests.

## 10. Review Log

Use this section during the digestion period. Add evidence, not speculative
solutions.

| Date | Observation | Severity | Candidate v0.6 item |
| --- | --- | --- | --- |
| 2026-07-14 | Initial migration completed; direction accepted, implementation concentration noted | planned | Sections A–E |
| 2026-07-20 | Generated inventory proves 44 Commands, 6 versioned sources, and 13 Providers have unique ownership and complete Runtime closure; Workspace snapshot diagnostics and frozen v0.5 fixtures added | pass | Slice 0 evidence |
| 2026-07-20 | Module and Platform Service snapshot replacement fixtures pass; Command history now retains resolved Contract source/version and Provider versions through migration 0044 | pass | Slice 0 evidence |
| 2026-07-20 | Contract types, registry, Runtime planning, structural validation, and Workspace persistence/diagnostics now have explicit internal boundaries behind the unchanged `command-contracts` public facade; dependency-boundary tests added | pass | Slice 1 responsibility split |
| 2026-07-20 | All 13 built-in Providers moved behind one explicit composition root and split into Assignment, Forms, FSM, Quote, Scheduling, and Workflow contexts; `forms.ts` no longer registers Providers as an import side effect | pass | Slice 1 Provider split |
| 2026-07-20 | Workspace execution resolution, snapshot persistence, and read-only diagnostics split into narrow modules behind the compatible `workspace.ts` facade; static Contract bridge reviewed and retained until backfill/repair exists | pass | Slice 1 Workspace split |
| 2026-07-20 | Explicit dry-run and idempotent repair classify sources as in-sync, missing, outdated, or conflicting; repair restores installed Module and Platform Service snapshots, fails closed on ownership conflicts, and ordinary Module installation no longer performs global Platform Service repair | pass | Slice 2 provisioning/repair |
| 2026-07-20 | Standalone upgrade-impact analysis was removed during acceptance refinement because no Module-upgrade mutation flow consumes it; snapshot replacement and ownership-conflict tests remain | deferred to `V06-D01` | v0.7.4 upgrade flow |
| 2026-07-20 | Pack uninstall now performs a complete preflight before destructive writes and blocks retained Contract capability consumers, Workflow Command consumers, Automation references, or Workflow history; safe removal also deletes source-owned Workflow definitions | pass | Slice 2 uninstall safety |
| 2026-07-20 | Fleet-wide Workspace Contract diagnostics and idempotent backfill now preflight every Workspace and block all writes on ambiguous ownership; the two process-level static Contracts and Runtime fallback were removed, so missing snapshots fail closed | pass | Slice 2 backfill and bridge removal |
| 2026-07-20 | Runtime now checks each transition handler's observed `audit.before.status` against `transition.from` before preparing or committing writes; negative tests cover missing and disallowed source evidence, and three Payment Contracts were corrected from false transitions to their actual create/action semantics | pass | Slice 3 transition source enforcement |
| 2026-07-20 | Runtime now checks `audit.after.status` against every scalar or enumerated `transition.to` target before persistence; missing and disallowed result evidence fail closed without command-specific exceptions | pass | Slice 3 transition target enforcement |
| 2026-07-20 | Critical aggregate-result postconditions now use two deliberately narrow executable assertion types—top-level field equality and non-null presence—covering completion timestamps and create-state/version invariants while cross-aggregate guarantees remain Provider-owned | pass | Slice 3 typed result assertions |
| 2026-07-20 | Every governed Command now authorizes its declared permission and allowed actor type at the Runtime boundary; user and API-key actors resolve through the existing Workspace/Organization membership and permission-group model, trusted system actors remain explicit, Agent actors fail closed by default, and duplicate API permission maps were reduced to non-governed legacy commands | pass | Slice 3 authorization boundary |
| 2026-07-20 | Form Submission draft creation/promotion, revision, return, and acceptance now use atomic affected-row guards; stale prepared writes roll back the complete Command batch and surface a version conflict, with focused races proving only one competing outcome commits | pass | Slice 3 Form Submission concurrency |
| 2026-07-20 | Providers now report semantic prepared-record counts independently from SQL statement counts; Runtime enforces Contract cardinality, every atomic Provider statement declares its commit-time affected-row expectation, and stale or partial effects roll back the complete batch | pass | Slice 3 Provider cardinality |
| 2026-07-20 | Negative journeys prove generic CRUD and Automation cannot update governed fields, while Workflow definitions cannot directly apply lifecycle changes or create Command history; authoritative transitions remain behind named Commands | pass | Slice 3 mutation-boundary closure |
| 2026-07-20 | Public SDK template and generated Contract-test APIs were removed during acceptance refinement because they had no external consumer; the concise authoring guide, CLI/Catalog validation, and real handler fixtures remain | deferred to `V06-D03` | v0.8.0 external consumer |
| 2026-07-20 | CI executes Catalog capability-closure and architecture-inventory gates; architecture tests constrain physical business-table writes to their owning handler context, and Workspace resolution errors identify the Command, source, missing capability, and repair action | pass | Slice 6 governance and diagnostics |
| 2026-07-20 | The three customization tiers remain a canonical decision guide; the premature Agent discovery Runtime/API and candidate-upgrade compatibility helper were removed until the Agent customization product has a concrete consumer | deferred to `V06-D01` / `V06-D04` | v0.7.4 upgrade / v0.8.0 Agent discovery |
