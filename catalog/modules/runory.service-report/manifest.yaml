id: runory.service-report
name: Service Report Management
version: 1.0.0
coreCompatibility: ">=0.1.0"

dependencies:
  - runory.work-order
  - runory.service-visit

objects:
  - key: service_report
    label: Service Report
    fields:
      - key: work_order_id
        label: Related Work Order
        type: text
        ownership: module_owned
        required: true
      - key: service_visit_id
        label: Related Visit
        type: text
        ownership: module_owned
      - key: summary
        label: Service Summary
        type: text
        ownership: module_owned
        required: true
      - key: resolution
        label: Resolution
        type: text
        ownership: module_owned
      - key: customer_signature
        label: Customer Signature
        type: text
        ownership: module_owned
      - key: photos
        label: Photos
        type: text
        ownership: module_owned
      - key: created_by
        label: Created By
        type: text
        ownership: module_owned
      - key: completed_at
        label: Completed At
        type: date
        ownership: module_owned

views:
  - object: service_report
    key: service_report_list
    type: list
    label: Service Report List
    config:
      columns:
        - field: work_order_id
          label: Related Work Order
        - field: summary
          label: Service Summary
        - field: created_by
          label: Created By
        - field: completed_at
          label: Completed At
      actions:
        - create
        - view
      pageSize: 20

  - object: service_report
    key: service_report_form
    type: form
    label: Service Report Form
    config:
      sections:
        - title: Basic Info
          fields:
            - field: work_order_id
              required: true
            - field: service_visit_id
            - field: summary
              required: true
        - title: Service Details
          fields:
            - field: resolution
            - field: customer_signature
            - field: photos
        - title: Completion Info
          fields:
            - field: created_by
            - field: completed_at

permissions:
  - service_report.read
  - service_report.create
  - service_report.update
  - service_report.delete

# ── Cross-Pack Relations (v0.2.4) ──
relations:
  - object: service_report
    targetObject: work_order
    targetModule: runory.work-order
    type: many_to_one
    foreignKey: work_order_id
    label: Related Work Order
  - object: service_report
    targetObject: service_visit
    targetModule: runory.service-visit
    type: many_to_one
    foreignKey: service_visit_id
    label: Related Visit

migrations:
  install: migrations/install.sql
  uninstallPolicy: retain_data

presentation:
  visibility: contextual
  surface: work_orders
  audience: [service_supervisor]

ui:
  navigation:
    - label: Service Report
      route: /service-reports
      icon: file-text
      sortOrder: 55

extensionPoints:
  entities:
    - entity: service_report
      customFields:
        enabled: true
        allowedTypes: [text, number, date, select, boolean]
        maxFields: 20
        reservedKeys: [id, work_order_id, service_visit_id, summary, resolution, customer_signature, photos, created_by, completed_at, created_at, updated_at]
      customRelations:
        enabled: false
  views:
    - view: service_report_list
      slots:
        - id: service_report.list.columns
          type: column_group
          allowedExtensions: [customField]
          risk: low
      allowReorder: true
      allowFilters: true
      allowPageSizeChange: true
    - view: service_report_form
      slots:
        - id: service_report.form.basic_fields.after
          type: field_group
          allowedExtensions: [customField]
          risk: low
      allowAddSection: true
      allowAddAction: true

dashboard:
  widgets:
    - key: recent_service_reports_list
      type: list
      label: Recent Service Reports
      icon: file-text
      tone: emerald
      data:
        kind: recent
        object: service_report
        orderBy: "completed_at desc"
        limit: 5
        columns: [work_order_id, summary, created_by, completed_at]
      link: /service-reports
      configurable:
        - path: data.limit
          label: Display Count
          type: number
          min: 1
          max: 20

upgradePolicy:
  supportsWorkspaceExtensions: true
  breakingChangePolicy: manual_review
