id: runory.ticket
name: Support Ticket
version: 1.0.0
coreCompatibility: ">=0.1.0"

dependencies:
  - runory.company
  - runory.contact
  - runory.task

objects:
  - key: ticket
    label: Ticket
    fields:
      - key: ticket_number
        label: Ticket Number
        type: text
        ownership: module_owned
        required: true
      - key: subject
        label: Subject
        type: text
        ownership: module_owned
        required: true
      - key: description
        label: Description
        type: text
        ownership: module_owned
      - key: status
        label: Status
        type: select
        ownership: module_owned
        required: true
        default_value: "new"
        validation:
          options: ["new", "open", "pending", "resolved", "closed", "escalated"]
      - key: priority
        label: Priority
        type: select
        ownership: module_owned
        required: true
        default_value: "medium"
        validation:
          options: ["low", "medium", "high", "urgent"]
      - key: channel
        label: Channel
        type: select
        ownership: module_owned
        required: true
        default_value: "email"
        validation:
          options: ["email", "phone", "web", "chat", "social"]
      - key: category
        label: Category
        type: text
        ownership: module_owned
        description: "Ticket category (e.g., billing, technical, warranty, inquiry)"
      - key: company_id
        label: Related Company
        type: text
        ownership: module_owned
      - key: contact_id
        label: Related Contact
        type: text
        ownership: module_owned
      - key: asset_id
        label: Related Asset
        type: text
        ownership: module_owned
      - key: work_order_id
        label: Related Work Order
        type: text
        ownership: module_owned
        description: "FSM work order created when onsite service is needed"
      - key: quote_id
        label: Related Quote
        type: text
        ownership: module_owned
      - key: task_id
        label: Related Task
        type: text
        ownership: module_owned
      - key: knowledge_id
        label: Resolving Knowledge Article
        type: text
        ownership: module_owned
        description: "Knowledge article that resolved this ticket"
      - key: sla_id
        label: SLA Policy
        type: text
        ownership: module_owned
      - key: assigned_to
        label: Assigned To
        type: text
        ownership: module_owned
      - key: resolved_at
        label: Resolved At
        type: date
        ownership: module_owned
      - key: closed_at
        label: Closed At
        type: date
        ownership: module_owned
      - key: owner
        label: Owner
        type: user
        ownership: module_owned
      - key: notes
        label: Notes
        type: text
        ownership: module_owned

views:
  - object: ticket
    key: ticket_list
    type: list
    label: Ticket List
    config:
      columns:
        - field: ticket_number
          label: Ticket Number
        - field: subject
          label: Subject
        - field: status
          label: Status
        - field: priority
          label: Priority
        - field: channel
          label: Channel
        - field: assigned_to
          label: Assigned To
      actions:
        - create
        - view
      pageSize: 20

  - object: ticket
    key: ticket_form
    type: form
    label: Ticket Form
    config:
      sections:
        - title: Basic Information
          fields:
            - field: ticket_number
              required: true
            - field: subject
              required: true
            - field: description
            - field: status
              required: true
            - field: priority
              required: true
            - field: channel
              required: true
            - field: category
        - title: Associations
          fields:
            - field: company_id
            - field: contact_id
            - field: asset_id
            - field: work_order_id
            - field: quote_id
            - field: task_id
            - field: knowledge_id
            - field: sla_id
        - title: Assignment & Time
          fields:
            - field: assigned_to
            - field: resolved_at
            - field: closed_at
            - field: owner
            - field: notes

permissions:
  - ticket.read
  - ticket.create
  - ticket.update
  - ticket.delete

# ── Cross-Pack Relations (v0.2.8) ──
relations:
  - object: ticket
    targetObject: company
    targetModule: runory.company
    type: many_to_one
    foreignKey: company_id
    label: Related Company
  - object: ticket
    targetObject: contact
    targetModule: runory.contact
    type: many_to_one
    foreignKey: contact_id
    label: Related Contact
  - object: ticket
    targetObject: task
    targetModule: runory.task
    type: many_to_one
    foreignKey: task_id
    label: Related Task
  - object: ticket
    targetObject: asset
    targetModule: runory.asset
    type: many_to_one
    foreignKey: asset_id
    label: Related Asset
  - object: ticket
    targetObject: work_order
    targetModule: runory.work-order
    type: many_to_one
    foreignKey: work_order_id
    label: Related Work Order
  - object: ticket
    targetObject: quote
    targetModule: runory.quote
    type: many_to_one
    foreignKey: quote_id
    label: Related Quote
  - object: ticket
    targetObject: knowledge
    targetModule: runory.knowledge
    type: many_to_one
    foreignKey: knowledge_id
    label: Resolving Knowledge Article
  - object: ticket
    targetObject: support_sla
    targetModule: runory.support-sla
    type: many_to_one
    foreignKey: sla_id
    label: SLA Policy

migrations:
  install: migrations/install.sql
  uninstallPolicy: retain_data

ui:
  navigation:
    - label: Tickets
      route: /tickets
      icon: life-buoy
      sortOrder: 60

extensionPoints:
  entities:
    - entity: ticket
      customFields:
        enabled: true
        allowedTypes: [text, number, date, select, boolean]
        maxFields: 10
        reservedKeys: [id, ticket_number, subject, description, status, priority, channel, category, company_id, contact_id, asset_id, work_order_id, quote_id, task_id, knowledge_id, sla_id, assigned_to, resolved_at, closed_at, owner, notes, created_at, updated_at]
      customRelations:
        enabled: false
  views:
    - view: ticket_list
      slots:
        - id: ticket.list.columns
          type: column_group
          allowedExtensions: [customField]
          risk: low
      allowReorder: true
      allowFilters: true
      allowPageSizeChange: true
    - view: ticket_form
      slots:
        - id: ticket.form.basic_fields.after
          type: field_group
          allowedExtensions: [customField]
          risk: low
      allowAddSection: true
      allowAddAction: true

dashboard:
  widgets:
    - key: open_tickets_metric
      type: metric_card
      label: Open Tickets
      icon: life-buoy
      tone: amber
      data:
        kind: count
        object: ticket
        where: "status IN ('new', 'open', 'pending')"
      link: /tickets

    - key: escalated_tickets_metric
      type: metric_card
      label: Escalated Tickets
      icon: alert-triangle
      tone: red
      data:
        kind: count
        object: ticket
        where: "status = 'escalated'"
      link: /tickets

    - key: resolved_tickets_metric
      type: metric_card
      label: Resolved Tickets
      icon: check-circle
      tone: emerald
      data:
        kind: count
        object: ticket
        where: "status = 'resolved'"
      link: /tickets

    - key: ticket_status_breakdown
      type: breakdown
      label: Ticket Status Distribution
      icon: pie-chart
      tone: cyan
      data:
        kind: group_count
        object: ticket
        groupBy: status

    - key: ticket_priority_breakdown
      type: breakdown
      label: Ticket Priority Distribution
      icon: pie-chart
      tone: red
      data:
        kind: group_count
        object: ticket
        groupBy: priority

    - key: ticket_channel_breakdown
      type: breakdown
      label: Ticket Channel Distribution
      icon: pie-chart
      tone: violet
      data:
        kind: group_count
        object: ticket
        groupBy: channel

    - key: recent_tickets_list
      type: list
      label: Recent Tickets
      icon: life-buoy
      tone: cyan
      data:
        kind: recent
        object: ticket
        orderBy: "created_at desc"
        limit: 8
        columns: [ticket_number, subject, status, priority, assigned_to]
      link: /tickets
      configurable:
        - path: data.limit
          label: Display Count
          type: number
          min: 1
          max: 20

    - key: escalated_tickets_list
      type: list
      label: Escalated Tickets
      icon: alert-triangle
      tone: red
      data:
        kind: recent
        object: ticket
        where: "status = 'escalated'"
        orderBy: "created_at desc"
        limit: 5
        columns: [ticket_number, subject, priority, assigned_to]
      link: /tickets
      configurable:
        - path: data.limit
          label: Display Count
          type: number
          min: 1
          max: 20

upgradePolicy:
  supportsWorkspaceExtensions: true
  breakingChangePolicy: manual_review
